# Dependencies
node_modules/

# Build output
dist/
build/
!app/src/modules/Build/
*.tsbuildinfo

# Environment
.env.local
.env.*.local

# Local MCP server registrations (project-scoped Claude Code config).
# Contains machine-local absolute paths so doesn't belong in git.
.mcp.json

# Supabase
supabase/.temp/
supabase/.branches/

# Brainstorm server state (volatile pid/status files)
.superpowers/**/state/

# IDE / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Local service-account keys (never commit)
.secrets/
*.sa.json

# SA key dropped at repo root
lila-bot.json

# Generated SQL output (contains customer PII)
scripts/out/

# Customer data — spreadsheets, docs, and seed/prototype dumps (PII, never commit).
# Kept on disk for local ops use; just not tracked.
*.xlsx
*.docx
fulfillment records/
/seeds.sql
/seed_data.json
/seeds_js.json
# Legacy prototype HTML dumps with rendered customer PII (root only;
# app/index.html — the Vite entry — is still tracked).
/index.html
/makelila-app.html
# 2026-06-08: lila-follow-up-calendar.html bundles 100+ customer names,
# emails, phones, and operator notes inline. Removed from HEAD but persists
# in commit 27668ce — a future history-scrub PR can purge if compliance
# posture requires it.
/lila-follow-up-calendar.html
